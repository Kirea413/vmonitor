# vmonitor セットアップガイド

スマートフォンを Windows PC の仮想モニターとして使うためのセットアップ手順です。

---

## 必要なもの

| 項目 | 要件 |
|---|---|
| PC | **Windows 10 バージョン 1809 (build 17763) 以上**、または Windows 11 |
| スマートフォン | **Android 7.0 (API 24) 以上**、または **iOS 13.0 以上** |
| 接続方法 | 同一 Wi-Fi ネットワーク、または USB ケーブル |
| 開発環境（ビルドする場合） | .NET 8 SDK、Flutter 3.10+、Visual Studio 2022+（C++ デスクトップ開発） |
| 開発環境（拡張モードを使う場合） | 上記に加えて Windows Driver Kit (WDK) |

### Windows のバージョンについて

下限を決めているのは次の 2 つです。使っている API の公式な要件から出しています。

| 使うもの | 下限 | 効かないと |
|---|---|---|
| 仮想ディスプレイドライバ (IddCx 1.2) | Windows 10 1709 / build 16299 | 拡張モードが使えない（ミラーは可） |
| Windows Ink のペン注入 (`CreateSyntheticPointerDevice`) | Windows 10 1809 / build 17763 | ペンだけ使えない（タッチは可） |
| .NET 8 | Windows 10 1607 | アプリが起動しない |

厳しいほうに合わせて **1809 (17763)** としています。

INF の `UmdfExtensions = IddCx0102` が IddCx 1.2 を指しており、
IddCx 1.2 は Windows 10 1709 (build 16299) から入っています。

1809 未満の環境ではペンの初期化が空振りしますが、そこで止まらずタッチへ
落ちるようにしてあります。自分の Windows のビルド番号は次で分かります。

```bash
winver
```

> **実機で確かめたのは Windows 11 (build 26200) の 1 台だけ**です。
> Windows 10 では動かしていません。上の表は API の要件であって、
> 動作確認の結果ではありません。

### Android のバージョンについて

APK が要求する下限は **API 24 (Android 7.0)** です。これより古い端末には
インストールできません。ビルドした APK のマニフェストで確認できます。

```bash
aapt2 dump badging app-release.apk
```

この下限は Flutter エンジン側の都合で決まっています。vmonitor 自体が使う
仕組みはもっと古くから在るもので、これが理由で下限が上がってはいません。

- USB 直結 (AOA / `UsbAccessory`) … API 12 から
- H.264 のハードウェアデコード (`MediaCodec`) … API 16 から
- 画面の明るさ固定 (`screenBrightness`) … API 1 から

新しい API を使っている箇所は `Build.VERSION.SDK_INT` で分岐させてあり、
古い端末では素通りします（USB 権限の受け取り方が API 31 / 33 で変わる、
低遅延デコードの指定が API 30 から、など）。

対応 CPU は `arm64-v8a` / `armeabi-v7a` / `x86_64` です。

> **実機で確かめたのは 2 台**です（作者の端末と友人の端末、いずれも
> 比較的新しい Android）。API 24〜25 (Android 7.x) の端末では動作を
> 確認していません。下限は「インストールできる」ことの保証であって、
> 「動く」ことの保証ではありません。

### iOS について

デプロイ先は **iOS 13.0** です。ビルドは GitHub Actions の macOS ランナーで
行い、無署名の IPA をリリースに置いています（`.github/workflows/ios-unsigned-ipa.yml`）。
Mac は要りません。

> **iPhone 17 で動作確認済み**です（映像・タッチ・画面回転）。
> 確かめたのはこの 1 台だけで、iPad では動かしていません。

| 機能 | Android | iOS |
|---|:-:|:-:|
| Wi-Fi 接続 | ✅ | ✅ |
| USB 直結 (AOA) | ✅ | ❌ 非対応 |
| 映像のデコード・表示 | ✅ | ✅ |
| タッチを PC へ送る | ✅ | ✅ |
| 画面を消させない / 明るさ固定 | ✅ | ⚠️ 未確認 |

実装上の違いが 3 点あります。

- **USB 直結は iOS では原理的にできません。** Android Open Accessory は
  Android 専用の仕組みです
- **映像の経路が違います。** Android はネイティブのビューへ直接描きますが
  ([renderer_view.dart](mobile-app/lib/renderer/renderer_view.dart) の `_useNativeView`)、
  iOS は Flutter のテクスチャを経由します。この経路は Android で計測したとき
  235〜278ms の遅れがありました。iOS のほうが遅いはずです
- **明るさ固定の効き方が違います。** Android はウィンドウ属性なので表示中
  だけ効きますが、iOS の `UIScreen.brightness` は端末全体の設定を書き換えます。
  元の値へ戻すようにしていますが、強制終了された場合は変わったままです

無署名 IPA はそのままでは端末に入りません。Sideloadly や AltStore で
自分の証明書を付ける必要があります。

---

## 動作モード

vmonitor には 2 つの表示モードがあります。

| モード | 内容 | 必要なもの |
|---|---|---|
| **ミラー（既定）** | PC のメイン画面をそのままスマホに表示する | なし。すぐ使える |
| **拡張** | スマホを 2 枚目のディスプレイとして増設する | 仮想ディスプレイドライバのインストール |

まずミラーモードで動作を確認し、拡張デスクトップが必要になってからドライバを入れる、という順序を推奨します。

仮想ディスプレイドライバは UMDF（ユーザーモード）で動くため、
セキュアブートを無効にする必要もテスト署名モードも要りません。

接続方法は 2 つあります。

| 接続 | 内容 | 必要なもの |
|---|---|---|
| **Wi-Fi** | 同じネットワーク上の PC を自動検出して繋ぐ | なし |
| **USB 直結** | ケーブルで直接繋ぐ。AOA を使うため adb は不要 | USB 直結用ドライバ（インストーラーが導入） |

---

## PC クライアントのセットアップ

### 1. ネイティブエンコーダーのビルド

映像は H.264 に圧縮して送るため、先にネイティブエンコーダー DLL をビルドします。
これがないと映像が 1 フレームも送信されません。

```cmd
msbuild pc-client\VMonitor.Encoder\VMonitor.Encoder.vcxproj /p:Configuration=Release /p:Platform=x64 /p:SolutionDir=%CD%\pc-client\
```

`pc-client\bin\x64\Release\VMonitor.Encoder.dll` が生成されます。
以降の `dotnet build` がこれを実行ディレクトリへ自動でコピーします。

> **必要な環境**: Visual Studio 2022 以降（C++ デスクトップ開発ワークロード）

### 2. ビルド

```cmd
cd pc-client
dotnet build VMonitor.slnx
```

### 3. テスト実行（任意）

```cmd
dotnet test VMonitor.slnx
```

### 4. アプリ起動

```cmd
dotnet run --project VMonitor.UI
```

起動すると `MainWindow` が表示され、接続候補のスマートフォンを待機します。

**起動時に自動で行われること:**
- mDNS サービス (`_vmonitor._tcp`) の登録
- TCP ポート 7979 での待ち受け

スマホが接続すると、メインディスプレイのミラー配信とタッチ入力の受け付けが始まります。

### 4. インストーラースモークテスト

ドライバとサービスが正常にインストールされているか確認します:

```powershell
powershell -ExecutionPolicy Bypass -File pc-client\scripts\smoke-test.ps1
```

`Smoke test result: PASSED` と表示されれば OK です。

---

## 仮想ディスプレイドライバ（拡張モードを使う場合のみ）

スマホを「2 枚目のディスプレイ」として使う場合だけ必要です。ミラー表示で足りるなら飛ばせます。

### 1. ドライバのビルドとテスト署名

```powershell
powershell -ExecutionPolicy Bypass -File driver\build-and-sign.ps1
```

`driver\dist\` に次の 4 ファイルが生成されます。

| ファイル | 内容 |
|---|---|
| `VMonitorVDD.dll` | ドライバ本体（署名済み） |
| `VMonitorVDD.inf` | インストール情報 |
| `vmonitorvdd.cat` | カタログ（署名済み） |
| `MyTestCert.cer` | 署名に使った自己署名証明書 |

### 2. インストール

`VMonitorSetup.exe` を実行すると（UAC の確認が出ます）、以下を自動で行います。

1. 署名証明書を「信頼されたルート証明機関」と「信頼された発行元」に取り込む
2. `pnputil /add-driver` でドライバを DriverStore に登録する
3. SetupAPI で `Root\VMonitorVDD` のデバイスノードを作成する

**セキュアブートを無効にする必要も、テスト署名モードを有効にする必要もありません。**

理由は VMonitorVDD が **UMDF（ユーザーモード）ドライバ** だからです。
ドライバ本体は `WUDFHost.exe` の中で動き、カーネルには読み込まれません。
テスト署名モードやセキュアブートが制御しているのはカーネルモードの署名強制（DSE）で、
ユーザーモードドライバはその対象外です。導入の条件は
「カタログの署名者をその PC が信頼しているか」だけなので、手順 1 で満たされます。

自分の PC に自分の証明書を信頼させているだけで、セキュリティ機能を迂回してはいません。
アンインストール時にはこの証明書も信頼ストアから取り除かれます。

> **手順 3 が必要な理由**: 仮想ディスプレイには対応する物理ハードウェアがありません。
> `pnputil /add-driver /install` は「既にあるデバイスに合うドライバを当てる」操作なので、
> デバイスノード自体は作られません。誰かが明示的に作らないと、
> DriverStore に登録されただけで仮想ディスプレイは現れません。

インストール後、Windows の「ディスプレイ設定」に仮想ディスプレイが追加されます。

> もし署名が拒否される場合に限り、テスト署名モードが必要になります。
> その場合はセキュアブートを無効にしたうえで `bcdedit /set testsigning on` を実行し、再起動してください。
> インストーラーは自動ではこの設定を変更しません。

---

## ファイアウォール

スマホから PC へ接続するには、次の受信を許可する必要があります。

| プロトコル | ポート | 用途 |
|---|---|---|
| TCP | 7979 | 映像・タッチの転送 |
| UDP | 5353 | mDNS によるデバイス探索 |

`VMonitorSetup.exe` がインストール時に `VMonitor.UI.exe` を対象とした受信規則を自動登録します。

開発中に `dotnet run` で直接起動する場合は、初回起動時に Windows が表示する
「ファイアウォールでブロックされました」のダイアログで **「アクセスを許可する」** を選んでください。
これを拒否すると、スマホから PC を見つけられず、手動 IP 接続も失敗します。

---

## スマホアプリのセットアップ

### 1. 依存関係のインストール

```cmd
cd mobile-app
flutter pub get
```

### 2. デバッグ実行（開発用）

```cmd
# Android
flutter run -d android

# iOS（Mac が必要）
flutter run -d ios
```

### 3. リリースビルド

```cmd
# Android APK
flutter build apk --release

# iOS
flutter build ios --release
```

ビルドされた APK / IPA を端末にインストールします。

---

## 接続手順

### Wi-Fi 接続（ワイヤレス）

1. PC とスマートフォンを **同じ Wi-Fi** に接続する
2. PC クライアントを起動する
3. スマホアプリを起動する → 同一ネットワーク上の PC が自動検出されてリストに表示される
4. リストから接続したい PC を選んで「接続」ボタンをタップ
5. PC 側に **「接続を許可しますか？」** ダイアログが表示されるので「はい」を選ぶ
6. セッションが確立され、スマートフォンに PC の映像が表示される

### USB 接続（有線・Android）

adb は必要ありません。開発者オプションを有効にする必要もありません。
AOA（Android Open Accessory）で端末を直接掴みます。

1. PC クライアントを起動する
2. USB ケーブルで PC とスマートフォンを接続する
3. PC が端末をアクセサリーモードへ切り替える
   → 端末側で vmonitor アプリが自動的に起動する
   （初回のみ「アクセサリーへのアクセスを許可しますか？」が出る。
     「常に起動する」にチェックを入れておくと次回から出ない）
4. PC 側に許可ダイアログが表示されるので「はい」を選ぶ
5. 映像が表示され、タッチ操作が PC に届くようになる

#### 繋がらないとき

**adb サーバーを止めてください。**

```
adb kill-server
```

端末の USB デバッグが有効だと、アクセサリーモードでも ADB のインターフェースが
同居した複合デバイスになります。この状態で adb サーバーが動いていると、
adb 側が USB を占有してしまい、vmonitor からはアクセサリー側を開けません。

Android Studio や `flutter run` は自動的に adb を起動するので、
USB 直結を試すときはそれらを終了しておいてください。

不明なデバイスがデバイスマネージャーに現れている場合は、
USB 直結用のドライバ（`VMonitorAOA.inf`）が入っていません。
インストーラーを再実行するか、管理者権限で次を実行してください。

```
pnputil /add-driver VMonitorAOA.inf /install
```

状態の確認には診断ツールを使えます。

```
vmonitor-doctor aoa
```

### USB 接続（有線・iOS / ADB を使う場合）

1. USB ケーブルで PC とスマートフォンを接続する
   - Android: 開発者オプション → USB デバッグを有効にする
   - iOS: 「このコンピューターを信頼しますか？」で「信頼」をタップ
2. PC 側で端末から PC へのトンネルを張る

   ```
   adb reverse tcp:7979 tcp:7979
   ```

   `forward` ではなく `reverse` です。`forward` は PC から端末への向きで、
   さらに PC 側の 127.0.0.1:7979 を奪ってしまい Wi-Fi 接続まで壊します。
3. スマホアプリの「ADB 経由で接続」をタップする

---

## ディスプレイ設定

接続後、PC クライアントの設定タブから以下を変更できます。

| 設定 | 内容 |
|---|---|
| **複製（クローン）** | PC 画面と同じ内容をスマートフォンに表示 |
| **拡張** | スマートフォンをサブディスプレイとして使用（デフォルト） |
| **セカンダリのみ** | PC メインディスプレイをオフにしてスマートフォンのみ使用 |
| **解像度** | プリセット一覧から選択、または幅×高さを手動入力 |

Windows の「ディスプレイ設定」からも操作できます。

---

## タッチ入力

接続中はスマートフォンの画面タッチが、Windows のポインター注入 API 経由で
**本物のタッチ入力**として PC に伝わります。

| 操作 | 挙動 |
|---|---|
| シングルタッチ | タップ・ドラッグとして PC に伝わる |
| マルチタッチ | 最大 10 点まで同時に伝わり、ピンチやスワイプが効く |
| ペンモード | 筆圧付きの Windows Ink ペン入力として注入され、OneNote などで手書きになる |

タッチとペンは内部で別の API を使い分けています。

- **タッチ**: `InitializeTouchInjection` + `InjectTouchInput`（Windows 8 以降）
- **ペン**: `CreateSyntheticPointerDevice` + `InjectSyntheticPointerInput`（Windows 10 1809 以降）

### 注入されない場合

管理者権限で動いているアプリ（タスクマネージャー、UAC ダイアログなど）へは、
通常権限のプロセスから入力を注入できません。Windows の仕様による制限です。
そうしたアプリを操作したい場合は vmonitor 自体を管理者権限で起動してください。

---

## 映像の性能特性

映像は Media Foundation の H.264 エンコーダーで圧縮しています。
実測のエンコード速度は概ね次のとおりです（環境により変動します）。

| 解像度 | エンコード速度 |
|---|---|
| 640x480 | 約 170 fps |
| 1280x720 | 約 60 fps |
| 1920x1080 | 約 30 fps |

1080p のミラーリングは 30fps 前後が上限になります。より滑らかにしたい場合は、
PC 側の画面解像度を下げるか、スマホアプリの設定でフレームレートを下げてください。

---

## 映像品質の設定

スマホアプリの設定画面（映像表示中に右上の ⚙ アイコン）から変更できます。

| 設定 | 範囲 | デフォルト |
|---|---|---|
| フレームレート | 30 / 45 / 60 fps | 60 fps |
| ビットレート | 1〜50 Mbps | 10 Mbps |

Wi-Fi 環境が不安定な場合はビットレートを下げると改善することがあります。帯域に応じた自動調整も有効です。

---

## 信頼済みデバイスの管理

PC クライアントの「信頼済みデバイス」タブから、過去に許可したスマートフォンの一覧を確認・削除できます。削除すると次回接続時に再度許可ダイアログが表示されます。

---

## エラーログ

PC クライアントの「設定・ログ」タブにログファイルのパスが表示されます。

```
%APPDATA%\vmonitor\logs\vmonitor.log
```

ログは JSON Lines 形式で記録され、10MB を超えると自動でローテーション（最大 5 世代）されます。

---

## トラブルシューティング

### まず診断ツールを実行する

不具合の切り分けには診断ツールが使えます。キャプチャ・エンコード・入力注入・
デバイス探索を実際に呼び出して、どこで止まっているかを表示します。

```cmd
dotnet run --project pc-client\VMonitor.Diagnostics
```

個別に確認したい場合は項目を指定できます。

```cmd
vmonitor-doctor capture
```

指定できる項目: `capture` / `encode` / `input` / `mdns`

出力例:

```
── H.264 エンコード ──
  [OK  ] ネイティブエンコーダー
  [OK  ] 出力形式 (H.264 SPS/PPS)
   640x480  :  138.0 fps (  7.2 ms/frame)
  1280x720  :   51.7 fps ( 19.3 ms/frame)
  1920x1080 :   25.9 fps ( 38.6 ms/frame)  ← 30fps 未満
```

### PC が検出されない（Wi-Fi）

mDNS はネットワーク環境に左右されやすく、次のような場合は探索が届きません。

- 公衆 Wi-Fi やゲスト用ネットワーク（端末間通信が遮断されている）
- PC とスマホが別のネットワーク（有線と Wi-Fi、2.4GHz と 5GHz の分離設定など）
- ファイアウォールが UDP 5353 の受信を塞いでいる

確認する順序:

1. PC とスマートフォンが同じ Wi-Fi ネットワークに接続されているか
2. PC のファイアウォールで vmonitor の通信が許可されているか
3. PC クライアントを再起動する

**それでも見つからない場合は手動 IP 接続を使ってください。** 探索に頼らず確実に繋がります。

1. PC で `ipconfig` を実行し、IPv4 アドレス（例: `192.168.1.23`）を確認する
2. スマホアプリの接続画面で IP アドレスとポート `7979` を入力する
3. 「手動接続」をタップする

### 映像が表示されない（接続はできる）

`VMonitor.Encoder.dll` が見つかっていない可能性が高いです。ログに
`映像が 1 フレームも送信されていません` と記録されます。

セットアップ手順 1 のネイティブエンコーダーのビルドを実行し、
`VMonitor.UI.exe` と同じフォルダに `VMonitor.Encoder.dll` があることを確認してください。

### 接続がタイムアウトする

- 10 秒以内に接続できない場合はタイムアウト通知が表示されます
- 「再試行」ボタンで再接続、または「別の PC を探す」で再スキャンできます

### 仮想ディスプレイが Windows に認識されない

ドライバの状態を確認します:

```powershell
pnputil /enum-drivers
```

`vmonitor` のエントリが表示されない場合は、PC クライアントを管理者権限で再起動してください。

### ドライバが停止した

PC クライアントが自動的に再起動を試みます（最大 3 回、5 秒間隔）。失敗した場合は通知が表示されるので、PC を再起動してください。

---

## 設定ファイルの場所

| ファイル | パス |
|---|---|
| 設定 | `%APPDATA%\vmonitor\settings.json` |
| エラーログ | `%APPDATA%\vmonitor\logs\vmonitor.log` |
| ドライバフラグ | `%ProgramData%\vmonitor\install-completed.flag` |
