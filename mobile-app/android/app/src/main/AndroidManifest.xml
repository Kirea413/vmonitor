<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Wi-Fi 接続に要る。7980 番での待ち受けと mDNS での PC 探索、
         どちらもこれが無いと SocketException になる。

         Flutter のひな形は debug と profile のマニフェストにしか
         これを書かない（ホットリロード用）。開発中はそちらが効くので
         気付かないが、release ビルドでは Wi-Fi 接続が丸ごと動かない。
         USB 直結だけが生き残るので、原因が分かりにくい。 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- USB アクセサリー (AOA) で PC と直結する。
         対応していない端末でも Wi-Fi 接続は使えるので required は false にする。 -->
    <uses-feature
        android:name="android.hardware.usb.accessory"
        android:required="false" />

    <application
        android:label="vmonitor"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- PC がこの端末をアクセサリーモードへ切り替えると、
                 accessory_filter.xml と一致する場合にこのアプリが起動する。
                 このとき USB の利用許可は自動で与えられ、確認ダイアログは出ない。 -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_ACCESSORY_ATTACHED"/>
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_ACCESSORY_ATTACHED"
                android:resource="@xml/accessory_filter"/>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
